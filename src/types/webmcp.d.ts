// Official WebMCP typings (global `WebMCP` namespace + `document.modelContext`). The package lives
// outside DefinitelyTyped, so it has to be referenced explicitly. See src/lib/webmcp/types.ts for how
// the strict spec shape is loosened to what real hosts provide.
/// <reference types="webmcp-types" />
